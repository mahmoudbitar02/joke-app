{
  "name": "witze-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass-watch": "sass --watch src/styles:outputs"
  },
  "devDependencies": {
    "sass": "^1.98.0",
    "vite": "^8.0.1"
  }
}
